# Ensure text files use Unix-style line endings
*.txt text eol=lf

# Mark images as binary
*.png binary
*.jpg binary

# Treat large files using Git LFS
*.psd filter=lfs diff=lfs merge=lfs -text

.DS_Store

